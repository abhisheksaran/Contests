#include <iostream>

using namespace std;

int main() {
	int n;
	cin >> n;	
	      
	int ans=0;
    for(int i=0;i<n-1;i++)                        //just using simple following formulae
    {
        ans=ans+((i*(i+1))/2)*((n-i)/2);
    }
	
	cout <<  ans << endl;		// Writing output to STDOUT
}
